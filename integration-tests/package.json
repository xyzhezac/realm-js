{
  "name": "@realm/integration-tests-root",
  "version": "0.1.0",
  "description": "A wrapper module that can install and run Realm JS tests in various environments",
  "private": true,
  "scripts": {
    "lint": "echo 'Nothing to lint'",
    "start": "npm start --prefix tests --"
  }
}